@model IList<Employee>
@{
    ViewData["Title"] = "ShowEmployees";
}

<h1>ShowEmployees</h1>
<p>There are now @ViewBag.sl employees</p>
@*<div data-role="tile">
        @foreach (var element in @Model)
        {
            <span>the name : @element.EmpName ,</span>
            <span>the phone : @element.PhoneNumber ,</span>
            <span>the city : @element.City</span>

            //@Html.ActionLink("Delete Employee", "Delete", "Next", new { listIds = Model.Select(m => m.EmpName).ToList() })
            @Html.ActionLink("Delete Employee", "Delete", "Next", new { id = element.EmpName }, null)
            <br />
        }
    </div>*@
<p>
    @Html.ActionLink("Add", "Create")
</p>
<form action="Search">
    <input type="text" placeholder="Enter a name " id="firstletter" name="firstletter">
    @*<input type="" value="Find">*@
    <input type="submit" value="Search" onclick="location.href='@Url.Action("Search", "Next")'" />
</form>
<table class="table">
    <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>City</th>

    </tr>

    @foreach (var item in Model)
    {
    <tr>
        @Html.Partial("_PertialEmployee",item)
    </tr>
    }

</table>
<div data-role="tile"></div>





